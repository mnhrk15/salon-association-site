/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration needed for now.
  // The 'images' configuration for remote patterns is removed
  // as we are now using local placeholder images.
};

module.exports = nextConfig; 